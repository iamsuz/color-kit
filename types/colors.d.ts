/**
 * Represents a color with a hex value and name.
 */
export interface Color {
	hex: string;
	name: string;
}
/**
 * Array of color objects with hex values and names.
 */
declare const colors: Color[];

export default colors;
