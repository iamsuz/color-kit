const { generateRandomColor } = require("./randomColor");
const { identifyColor, hexToRgb } = require("./identifyColor");

module.exports = {
    generateRandomColor,
    identifyColor,
    hexToRgb
};